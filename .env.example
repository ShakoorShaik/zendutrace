# Form relay endpoint for email capture (FormSubmit/Formspree/serverless function).
# Production uses FormSubmit — set the recipient (or its FormSubmit random alias):
# VITE_CAPTURE_ENDPOINT=https://formsubmit.co/ajax/you@example.com
VITE_CAPTURE_ENDPOINT=
